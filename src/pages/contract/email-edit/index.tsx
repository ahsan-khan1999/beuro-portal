import EmailForm from "@/components/contract/EmailForm";
import { Layout } from "@/layout";
import React from "react";

const index = () => {
  return (
    <Layout>
      <EmailForm />
    </Layout>
  );
};

export default index;
